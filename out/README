This directory is used to store permanent artifacts such as binaries, analysis results, etc. generated by the docker containers.
By default, run.sh mounts this directory to /out/ within the active containers.
